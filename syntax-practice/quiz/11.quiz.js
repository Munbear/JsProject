function stringChallenge(str) {
    let arr = []
    let result = 0;
    let convertNum = [{
        'zero' : 0,
        'one' : 1,
        'two' : 2,
        'three' : 3,
        'four' : 4,
        'fix' : 5,
        'six' : 6,
        'seven' : 7,
        'eight' : 8,
        'nine' : 9,
    }]
    let operator = {
        'plus' : '+',
        'minus' : 'negative',
    }
    convertNum.map( (value, index) => {
        conso
    })

}

console.log(stringChallenge("onezeropluseight"))

// "onezeropluseight"
// one zero plus eight
// outPut : oneeight
// "oneminusoneone"
// one minus one one
// outPut : negative one zero